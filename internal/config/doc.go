// Package config handles orca configuration loading and validation.
// It reads orca.yaml from the repository root and user-level config from
// ~/.config/orca/config.yaml, merging them with environment variable overrides.
package config
